{
  "scripts": {
    "watch": "chokidar 'src/**/*.elm' -c 'npm run build' --initial",
    "build": "elm make src/Main.elm --output elm.js"
  },
  "devDependencies": {
    "chokidar-cli": "^1.2.0"
  }
}
