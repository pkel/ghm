{$base_uri} {
  root webroot/
  redir /api /api/
  proxy /api/ localhost:3000 {
    without /api
  }
  redir /swagger /swagger/
  proxy /swagger/ localhost:3001 {
    without /swagger
  }
  fastcgi / localhost:9000 php {
    root /var/www/html
  }
}
